export class Journal {
    public name: string;
    public title: string;
    public similarity: string;

    constructor(name: string, title: string, similarity: string) {
        this.name = name;
        this.title = title;
        this.similarity = similarity;
    }
}
